require 'spec_helper'

describe "quartermaster" do
	# let!(:qm) { Quartermaster.new([1]) }
	# subject { qm }
	# describe "attributes" do
	# 	# it { should respond_to :save_public_bulk }
	# 	# it { should respond_to :save_private_bulk }
	# end
	# describe "must provide valid bitcoin key array" do
	# 	describe "empty" do
	#   	it {expect {Quartermaster.new('')}.to raise_error(RuntimeError, 'Empty Key Array')}
	#   	it {expect {Quartermaster.new([])}.to raise_error(RuntimeError, 'Empty Key Array')}
	#   	it {expect {Quartermaster.new()}.to raise_error(ArgumentError,'wrong number of arguments (0 for 1)')}
	# 	end
	# 	describe "invalid" do
	# 		it {expect {Quartermaster.new([1])}.to raise_error(RuntimeError, 'Invalid keys')}
	# 	end
		
		
	# end

	# describe "valid private key" do
	# 	# let!(:key) { Bitcoin::Key.generate.to_base58 }
	# 	let!(:fake) { 1 }
	# 	describe "description" do
	# 		it { valid_private_key?('assaf').should be_true }
	# 	end
	# end

end